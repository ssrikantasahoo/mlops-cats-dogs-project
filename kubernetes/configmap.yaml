# ConfigMap for application configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: cats-dogs-config
data:
  MODEL_PATH: "/app/models/model.pt"
  LOG_LEVEL: "INFO"
  PROMETHEUS_ENABLED: "true"

---
# Secret for sensitive configuration (example)
apiVersion: v1
kind: Secret
metadata:
  name: cats-dogs-secrets
type: Opaque
data:
  # Base64 encoded values
  # echo -n 'value' | base64
  API_KEY: YXBpLWtleS1oZXJl
